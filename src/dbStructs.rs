/* This module defines the structs used for our database and defines what info returned from Umler should be formatted as
* Additional info: Umler may return several columns we have no use for since this is an WSDL request and NOT a database query where we can specify 
* which columns we need. */